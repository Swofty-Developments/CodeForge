<plan>
  <phase>1</phase>
  <plan-number>03</plan-number>
  <type>execute</type>
  <name>View Module Skeleton & Navigation</name>
</plan>

<objective>
Create the view module structure with placeholder implementations for all major views (sidebar, chat, composer, tabs, settings). Wire up basic navigation between views.
Traces to: PRD §5 Screen Map
</objective>

<context>
- PRD: .planning/specs/PRD.md §5
- Plan 01-02 must be complete
</context>

<tasks>
  <task type="auto">
    <name>Create view modules</name>
    <files>
      crates/app/src/views/mod.rs
      crates/app/src/views/sidebar.rs
      crates/app/src/views/chat.rs
      crates/app/src/views/composer.rs
      crates/app/src/views/tabs.rs
      crates/app/src/views/settings.rs
    </files>
    <action>
      Each view module exports a `view()` function that takes &AppState and returns Element<Message>.

      sidebar.rs:
      - Renders project groups (placeholder list)
      - Thread items under each project
      - New thread button at bottom

      chat.rs:
      - Scrollable message list (empty state: "Start a conversation")
      - Each message: role indicator + content text

      composer.rs:
      - Text input for message
      - Send button
      - Provider selector (Claude/Codex dropdown or toggle)

      tabs.rs:
      - Horizontal tab bar
      - Each tab: thread title + close button
      - Active tab highlighted

      settings.rs:
      - Modal/overlay panel
      - Provider path inputs (claude binary, codex binary)
      - Default approval mode selector
      - Close button
    </action>
    <verify>
      - [ ] All view modules compile
      - [ ] Views are wired into app.rs view()
    </verify>
    <done>All view skeletons created and rendering</done>
  </task>

  <task type="auto">
    <name>Wire navigation messages</name>
    <files>crates/app/src/app.rs, crates/app/src/message.rs</files>
    <action>
      Add message handling in update():
      - SelectThread(id): set active_thread, show chat view
      - NewThread: create placeholder thread, switch to it
      - OpenSettings: toggle settings overlay
      - CloseSettings: hide settings overlay
      - SwitchTab(id): change active tab
      - CloseTab(id): remove tab

      Update view() to compose views based on state:
      - Always show sidebar (if visible)
      - Show tab bar + chat + composer for active thread
      - Show settings as overlay when open
    </action>
    <verify>
      - [ ] Can click between placeholder threads
      - [ ] Settings overlay toggles
      - [ ] Tab switching works
    </verify>
    <done>Navigation between views works</done>
  </task>
</tasks>

<verification>
  - [ ] `cargo build` succeeds
  - [ ] App renders all views in correct layout
  - [ ] Navigation between threads/settings works
  - [ ] No panics on any interaction
</verification>

<success_criteria>
App has a complete UI skeleton: sidebar with placeholder threads, tab bar, chat area with empty state, composer with text input, and toggleable settings panel. All dark themed and navigable.
</success_criteria>
