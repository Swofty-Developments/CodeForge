<plan>
  <phase>1</phase>
  <plan-number>02</plan-number>
  <type>execute</type>
  <name>Dark IDE Theme & App Shell Layout</name>
</plan>

<objective>
Implement the dark IDE-like theme and create the app shell layout with sidebar + main panel using iced's pane_grid or column/row layout. This establishes the visual foundation.
Traces to: PRD §5 Screen Map, PRD §6 (dark IDE-like theme)
</objective>

<context>
- PRD: .planning/specs/PRD.md §5 Screens & Navigation
- Discovery: .planning/phases/01-scaffolding/DISCOVERY.md
- Plan 01-01 must be complete
</context>

<tasks>
  <task type="auto">
    <name>Create theme module</name>
    <files>crates/app/src/theme.rs</files>
    <action>
      Create a custom iced Theme with dark IDE palette:
      - Background: #1e1e2e (dark blue-gray, Catppuccin Mocha inspired)
      - Surface: #313244 (slightly lighter for panels)
      - Primary: #89b4fa (blue accent)
      - Text: #cdd6f4 (light gray)
      - Subtext: #a6adc8 (dimmer text)
      - Success: #a6e3a1 (green)
      - Danger: #f38ba8 (red/pink)
      - Warning: #fab387 (orange)
      - Border: #45475a

      Define as iced custom Theme or use Theme::custom() with Palette.
      Export color constants for reuse.
    </action>
    <verify>
      - [ ] theme.rs compiles
      - [ ] Colors are defined as constants
    </verify>
    <done>Dark theme module created</done>
  </task>

  <task type="auto">
    <name>Create app shell with sidebar + main area</name>
    <files>crates/app/src/app.rs, crates/app/src/main.rs</files>
    <action>
      Refactor main.rs into app.rs with proper iced Application:

      - App struct with state: active_view, sidebar_width
      - Message enum: ToggleSidebar, SelectThread(ThreadId), NewThread, OpenSettings
      - view(): Row layout with:
        - Left: sidebar container (250px wide, surface background, border-right)
          - Header: "CodeForge" title
          - Placeholder: "No projects yet" text
          - Bottom: "New Thread" button + "Settings" gear icon
        - Right: main content area (fills remaining space)
          - Placeholder: "Select a thread or start a new one" centered text
      - Apply dark theme to window
      - Window settings: title "CodeForge", size 1200x800, min_size 800x500
    </action>
    <verify>
      - [ ] App shows two-panel layout
      - [ ] Sidebar has dark surface background
      - [ ] Main area has darker background
      - [ ] Window title is "CodeForge"
    </verify>
    <done>App shell with sidebar layout renders correctly</done>
  </task>

  <task type="auto">
    <name>Create message and state modules</name>
    <files>crates/app/src/message.rs, crates/app/src/state.rs</files>
    <action>
      message.rs:
      - pub enum Message with initial variants (UI events, future agent events)
      - Organized with nested enums: Message::Sidebar(SidebarMessage), Message::Chat(ChatMessage), etc.

      state.rs:
      - pub struct AppState with fields for UI state
      - threads: Vec<Thread>, active_thread: Option<ThreadId>
      - sidebar_visible: bool
      - Stub Thread and ThreadId types (will move to persistence later)
    </action>
    <verify>
      - [ ] Modules compile
      - [ ] Message enum is exhaustive for current UI
    </verify>
    <done>Message and state modules created</done>
  </task>
</tasks>

<verification>
  - [ ] `cargo build` succeeds
  - [ ] `./dev.sh` opens window with dark theme
  - [ ] Sidebar and main area are visually distinct
  - [ ] Window is resizable with min size enforced
</verification>

<success_criteria>
App opens with a dark IDE-like two-panel layout: sidebar on left with header and placeholder text, main area on right with centered placeholder. All dark themed with the Catppuccin-inspired palette.
</success_criteria>
